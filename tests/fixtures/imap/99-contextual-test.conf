# Deterministic credentials and Maildir layout for this disposable fixture only.
mail_location = maildir:~/Maildir

passdb {
  driver = static
  args = password=password
}

userdb {
  driver = static
  args = uid=1000 gid=1000 home=/srv/mail/%Lu
}
